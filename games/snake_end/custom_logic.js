/**
 * Hier könnt Ihr Variablen und Funktionen verändern, um das Spielverhalten beeinflussen.
 * Was Ihr hier definiert, wird in der Datei "snake.js" verwendet.
 */


/**
 * Hintergrundbild ändern.
 * 
 * In Datei "style.css" -> Zeile 26 wieder hinzufügen (die Kommentarzeichen '//' entfernen)
 */

/**
 * Farbe des Scores ändern.
 * 
 * In Datei "style.css" -> Zeile 9: color von "black" auf "white"
 */


/**
 * Farbe der ersten Schlange ändern.
 * 
 * In Datei "style.css" -> Zeile 30: background-color auf white
 */


/**
 * Schlange(n) hinzufügen.
 * 
 * Zweite Schlange hinzufügen:
 * In "snake.js" -> Zeile 67 wieder hinzufügen (die Kommentarzeichen '//' entfernen):
 * 
 * Dritte Schlange hinzufügen:
 * In "snake.js" -> Zeile 67:
 * 1) Neue Zeile hinzufügen 
 * 2) createSnake(40, 80, 0, Keys.N, Keys.M, Keys.B, 'snake3'),
 * 
 */


/**
 * Schlange größer machen.
 * 
 * In "styles.css" -> Zeile 30: width/heigth von 10px auf 15px
 */


/**
 * Mehr / Weniger Essen.
 * 
 * In "snake.js" -> Zeile 71/72: "i < 10" -> "i < 20" oder "i < 5"
 */


/**
 * Reaktivität der Schlange verändern.
 * Hierfür passen wir die Zeitintervalle an, in denen die Schlange sich bewegt.
 * 
 * In "snake.js" -> Zeile 255/256: timeout = window.setTimeout(loop, 3000 / speed + oldTime - time);
 * -> timeout = window.setTimeout(loop, 2000 / speed + oldTime - time);
 */


/**
 * Unverwundbarkeit der Schlange.
 */